version: 1
interface:
  display_name: YSS DB2MyBatis
  short_description: 从 MySQL/Oracle/PostgreSQL 元数据生成 YSS MyBatis 持久层脚手架
  default_prompt: 读取数据库元数据并生成指定模块的 domain gateway、repository 和 gateway impl，优先使用脚本命令并先 dry-run 验证。
